[project]
name = "roborev-documentation"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "zensical==0.0.53",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "mdformat==1.0.0",
    "mdformat-front-matters==2.0.0",
    "mdformat-mkdocs==5.3.0",
]
